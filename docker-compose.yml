services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: coach_diff
      POSTGRES_USER: coach_diff
      POSTGRES_PASSWORD: coach_diff
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: